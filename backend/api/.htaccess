RewriteEngine On
RewriteRule ^config\.php$ - [F,L]

# Pass the Authorization header through to PHP (Apache strips it by default)
RewriteCond %{HTTP:Authorization} .
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
